# Where the FastAPI backend is reachable from the Next.js server (used by the
# /api proxy in next.config.ts).
BACKEND_URL=http://localhost:8080

# Optional. Set this to call the backend directly from the browser instead of
# going through the proxy. Leave blank to use the proxy (recommended).
NEXT_PUBLIC_API_URL=
